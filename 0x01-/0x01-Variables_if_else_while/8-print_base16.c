#include <stdio.h>

void main()
{
    int hexa, alpha;

    for (hexa = 48; hexa <= 57; hexa++)
        putchar(hexa);
    for (alpha = 97; alpha <= 102; alpha++)
        putchar(alpha);
    putchar(10);
}